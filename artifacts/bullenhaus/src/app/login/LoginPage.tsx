// The /login route now renders the full trading-themed marketing page, which
// hosts the dropdown login panel (see marketing/MarketingLoginPage.tsx).
// Re-exported here so the App.tsx route import stays unchanged.
export { default } from './marketing/MarketingLoginPage';
