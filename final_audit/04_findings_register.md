# 04 — Findings Register

Repo-wide sweep for incomplete-implementation markers (`TODO`, `FIXME`, `XXX`, `mock data`,
`not implemented`) across `artifacts/bullenhaus/src` and `api/` returned **zero matches**. The
only "hardcoded" hits are a deliberate, commented fallback constant for one premarket asset
(`PreMarket.tsx:241`, "fall back to hardcoded" when the admin-managed asset record is
unavailable) — not a stub. This is corroborating (not exhaustive) evidence against
placeholder/demo-only behavior in the code that was read.

| ID | Severity | Priority | Layer | Confirmed Problem | Evidence | Impact | Root Cause | Minimal Fix | Verification |
|---|---|---|---|---|---|---|---|---|---|
| AUDIT-001 | P0 | 1 | Deployment/Build | `api/kyc/analyze-external.ts` failed to compile in Vercel's remote TS build (`getUser` missing on `SupabaseAuthClient`; `Response.ok/.status/.json()` missing), blocking PR #26 from shipping | User-pasted Vercel build log (4x `TS2339`); local `tsc --noEmit -p api/tsconfig.json` passed cleanly, proving the failure was environment-specific | Deploy-blocking; KYC review AI triage endpoint would 404/fail entirely in production if merged in this state | `api/tsconfig.json` had `lib: ["es2022"]` with no `"dom"`, so global `fetch`/`Response` typing depended entirely on `@types/node`'s ambient declarations resolving identically in Vercel's build as locally; Supabase auth-js types reference those same globals internally, so a resolution mismatch cascades into unrelated-looking member errors like `getUser` | Added `"dom"` to `lib` in `api/tsconfig.json` (commit `d6e940b`) | Local `tsc --noEmit` clean before and after; Vercel bot PR comment reported "Ready" (build success) on the commit containing this fix |
| AUDIT-002 | **P0** | 1 | Database/Security | 7 tables (`support_tickets`, `ticket_comments`, `tasks`, `messages`, `message_templates`, `sales_scripts`, `lead_stage_history`) had RLS **policies defined but RLS itself disabled** — the policies were completely inert; PostgREST would serve these tables to any role holding a table grant with no row-level restriction at all | Supabase security advisor, `ERROR` level, `rls_disabled_in_public` + `policy_exists_rls_disabled` for all 7 tables; confirmed independently via `pg_class.relrowsecurity = false` before the fix | Full exposure of client support tickets (PII/complaints), internal staff-client messages, CRM tasks, sales scripts, and lead pipeline history to any authenticated (and possibly anonymous, depending on grants) request | Tables were created with RLS policies but the `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` step was never run — likely because there is no tracked migration history for this schema (see AUDIT-009), so this step was silently skipped at some point | `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` for all 7 tables (migration `enable_rls_on_exposed_tables_and_fix_ticket_comments_idor`, applied to production) | Re-ran the security advisor after the fix: all 7 `rls_disabled_in_public`/`policy_exists_rls_disabled` `ERROR` findings are gone; confirmed via `pg_class.relrowsecurity = true` for all 7 tables |
| AUDIT-003 | **P1** | 3 | Database/Security | `ticket_comments_read` policy allowed **any** authenticated user with role `client` to read comments on **any** ticket, not just their own (no `ticket_id` ownership check); `ticket_comments_insert` had the same gap, letting a client insert a comment into any ticket | Read the live policy definition via `pg_policies` before the fix: `qual` for the read policy checked only `u.role = 'client'` (or staff), with no join back to `support_tickets.client_id` | Horizontal privilege escalation / IDOR: any client account could read internal staff replies and other clients' support correspondence on tickets they don't own, and could inject fabricated comments into other clients' tickets | Policy was written checking only the caller's role, not the caller's relationship to the specific row | Replaced both policies to additionally require `EXISTS (SELECT 1 FROM support_tickets st WHERE st.id = ticket_comments.ticket_id AND st.client_id = auth.uid())` for the client case, while leaving staff access unchanged (same migration as AUDIT-002) | Read back the new policy definitions after `apply_migration`; staff role branch preserved verbatim so no regression to the admin support-reply flow |
| AUDIT-004 | P2 | 7 | Database/Security | `premarket_contracts` storage bucket had a `SELECT` policy on `storage.objects` granted to role `public` (anon + authenticated), allowing directory listing of every file in the bucket | Supabase security advisor, `WARN`, `public_bucket_allows_listing` | Enumeration of signed-contract filenames/paths across all users (metadata leak: who signed what, when) via `storage.from(bucket).list()` or a direct REST query, even though nobody could read file *contents* without the URL | Bucket is `public=true` (object GET already bypasses RLS by design), so the extra `SELECT` policy on `storage.objects` was unnecessary for the app's actual usage — confirmed by code: the client only ever reads `contract_url` (an admin-managed template URL) and never lists the bucket | Replaced the `public`-role listing policy with one scoped to `authenticated` + `get_my_role() IN ('admin','superadmin','trade_admin')`, matching the bucket's existing INSERT/UPDATE/DELETE policies | Confirmed the client code path (`PreMarket.tsx`) never calls `.list()`; the one `.list()` caller (`AdminPreMarket.tsx`, a reachability health-check) runs as an authenticated admin and is still covered by the new policy. **Live HTTP verification of the public-URL-still-works claim was attempted but NOT_AVAILABLE** — this sandbox's egress proxy returned 403 for both `curl` and `WebFetch` against the Supabase host and the Vercel preview host (confirmed with `example.com` as a control, also 403'd) |
| AUDIT-005 | P3 | 9 | Database/Security | 6 functions (`set_updated_at`, `set_workflows_updated_at`, `sign_premarket_contract`, `execute_spot_trade`, `get_my_gamification_stats`, `get_gamification_leaderboard`) had a mutable `search_path` | Supabase security advisor, `WARN`, `function_search_path_mutable` | Low: theoretical search_path-hijack surface for `SECURITY DEFINER` functions if a lower-privileged role could create shadowing objects earlier in the resolution path; all bodies already schema-qualify their table references, limiting real exploitability | Functions were created without `SET search_path` | `ALTER FUNCTION ... SET search_path = public` for all 6 (metadata-only change, no logic touched) | Re-ran advisor after the fix — these 6 no longer appear in `function_search_path_mutable` |
| AUDIT-006 | P3 | 10 | Database/Security | Several `SECURITY DEFINER` functions (`approve_deposit`, `approve_withdrawal`, `assign_leads_bulk`, `execute_spot_trade`, `get_my_gamification_stats`, `get_gamification_leaderboard`, `ai_settings_status`, `get_my_role`, `handle_new_user`, `rls_auto_enable`, `sign_premarket_contract`) are `EXECUTE`-grantable by `anon`/`authenticated` | Supabase security advisor, `WARN`, `anon_security_definer_function_executable` / `authenticated_security_definer_function_executable` | **Investigated and found non-exploitable in every case, not fixed**: `approve_deposit`/`approve_withdrawal`/`assign_leads_bulk` self-reject non-staff callers via `get_my_role()` checks; `execute_spot_trade`/`sign_premarket_contract`/`get_my_gamification_stats` are `auth.uid()`-scoped to the caller's own data; `get_gamification_leaderboard`/`ai_settings_status`/`get_my_role` are intentionally-public read-only status/leaderboard data; `handle_new_user`/`rls_auto_enable` are `RETURNS trigger`/`RETURNS event_trigger` respectively, which Postgres refuses to execute outside a trigger-firing context regardless of GRANTs | n/a (verified safe by reading every flagged function's full body) | **Not fixed** — revoking `EXECUTE` from these would be defense-in-depth hardening, not a fix for an actual vulnerability, and touching grants on financial-approval functions without a live regression test carries more risk than the WARN-level advisory justifies in this pass | Full function bodies read via `pg_get_functiondef`; documented per-function reasoning above |
| AUDIT-007 | P2 | — | Auth | Supabase Auth "leaked password protection" (HaveIBeenPwned check) is disabled | Supabase security advisor, `WARN`, `auth_leaked_password_protection` | Users can set passwords already known to be compromised in public breach corpora | Project-level Auth setting, off by default | **Not fixed this session** — this is a Supabase Auth *dashboard* setting, not something `apply_migration`/`execute_sql` can toggle; requires the project owner to enable it in Supabase Auth settings (Authentication → Policies) | N/A — flagged for owner action, see 09/10 |
| AUDIT-008 | P2 | — | Repo hygiene | Root `.gitignore` did not exclude `.env*` patterns | Read `.gitignore`; confirmed via `git log --all --diff-filter=A --name-only` that no `.env` file was ever actually committed (only `artifacts/bullenhaus/.env.example` is tracked) | Not yet exploited, but any future local `.env` would be tracked by default under the pre-fix `.gitignore` | Pattern simply missing from the ignore list | Added `.env` / `.env.*` (with `!.env.example` carve-out) to `.gitignore` (commit `d85e4c1`) | `git status` shows the new lines tracked; no behavior change to the build |
| AUDIT-009 | P2 | — | Repo/DevOps | No database migration files are tracked in the repository (`supabase/migrations/` is empty); all schema history exists only as live state in the production Supabase project | `find supabase/migrations -type f` → 0 files; every fix in this audit had to be applied directly via `apply_migration` against production with no staging/branch DB to rehearse on first | No reviewable history, no rollback path from git, no way to reconstruct the schema from source, no code-review step for schema changes going forward | Schema was evidently built up via ad-hoc `apply_migration`/dashboard SQL over time rather than a migration-file workflow | **Not fixed this session** — this is a process/tooling gap, not a single patchable bug; fixing it means adopting a migration-file workflow going forward (e.g., `supabase db diff` from here on), which is a decision for the project owner, not a minimal safe patch | Flagged for owner action, see 10_handover_runbook.md |
| AUDIT-010 | P3 | — | Documentation | `replit.md` (the only would-be project doc) is entirely unfilled boilerplate; no `README.md` exists at repo root | Read the file in full — every section still contains its placeholder instruction sentence | New contributors/AI agents have no authoritative onboarding doc; this audit had to reconstruct purpose/architecture from the code itself (see 03_requirements_traceability.md) | Template was never filled in after scaffolding | **Not fixed this session** — writing real product documentation is a content task outside "minimal fix for a confirmed bug", and doing it well requires product-owner input this audit can't originate on its own | Flagged for owner action |
| AUDIT-011 | P3 | — | Repo hygiene | Repo root has ~10 stray files from an earlier ad-hoc audit/migration pass (`AUDIT_REPORT.md`, `PROJECT_AUDIT.md`, `AUDIT_CANDIDATES.md`, `apply_migrations_phase*.sql`, `bullenhaus_full_migration.txt`, `git_status*.txt`, etc.) | `ls` on repo root | Clutter/confusion only; no functional impact; some of these files may contain a point-in-time snapshot of schema/data someone intended to keep | Accumulated over time, never cleaned up | **Not fixed this session** — deleting files the audit didn't create requires confirming none of them are still-needed reference material; this is exactly the "don't overwrite/delete a user's in-progress work without checking" case in the operating rules, not a safe unilateral cleanup | Flagged for owner decision, not deleted |
| AUDIT-012 | — | — | Testing/CI | No automated tests, no ESLint config, no CI workflows exist anywhere in the repository | `find` for `*.test.*`/`*.spec.*` → 0; `find` for eslint config → 0; `.github/workflows` → does not exist | No regression safety net beyond `tsc`/`vite build`; every fix in this audit had to be verified by manual reasoning + typecheck/build rather than an automated suite | Never set up | **Not fixed this session** — scaffolding a real test suite and CI pipeline is a scope decision for the project owner (what to test, what CI provider), not a "confirmed bug" this audit can minimally patch | Flagged as a structural gap, see 06/10 |

## Findings closed this session: **6** (AUDIT-001 through AUDIT-005, plus AUDIT-006 investigated-and-cleared)
## Findings requiring owner action (cannot be closed by an agent in this environment): AUDIT-007, AUDIT-011
## Open P0: **0** (both P0s — AUDIT-001, AUDIT-002 — closed and re-verified)
## Open P1: **0** (AUDIT-003 closed and re-verified)
## Open blocking P2: **0** (remaining P2s are owner-action items, not blocking a merge/deploy — see 11_final_release_report.md for the reasoning)

## Interim verdict: **REPAIR_REQUIRED → executed → READY_FOR_FINAL_VERIFICATION**

---

## Loop 2 — structural-debt closure (follow-up pass)

After the initial repair loop, a second pass drove the "requiring owner action" items down as
far as this environment allows, and empirically verified the RLS/IDOR fixes:

| ID | Prior status | Loop-2 action | New status |
|---|---|---|---|
| AUDIT-002 / AUDIT-003 | fixed (advisor-verified) | **Empirically verified** via SQL role impersonation: anon sees 0 rows across all 7 tables; a non-owner client sees 0 ticket_comments/tickets (IDOR closed); the ticket owner sees exactly their 1 comment/1 ticket; a staff agent retains full access (scripts=9, templates=5, messages=1, etc. — no regression) | CONFIRMED empirically |
| AUDIT-009 (no tracked migrations) | owner-action | Committed this session's 3 security migrations as tracked SQL under `supabase/migrations/` + a README documenting the partial-backfill state and go-forward file-first workflow | CLOSED (partial backfill; full historical baseline dump still needs a networked env — documented) |
| AUDIT-010 (unfilled docs) | owner-action | Filled `replit.md` with real stack, repo map, architecture decisions, gotchas, product scope | CLOSED |
| AUDIT-012 (no tests/lint/CI) | owner-action | Added GitHub Actions CI (`.github/workflows/ci.yml`: typecheck + api typecheck + lint + build + test); added a real test harness (`node:test` + tsx) with 9 security-relevant KYC-sanitizer tests; added correctness-focused ESLint (0 errors, 106 advisory warnings) wired into CI; added `.vercelignore` so test files aren't deployed as functions | CLOSED |
| AUDIT-007 (leaked-password protection) | owner-action | Confirmed this is a Supabase Auth **dashboard** setting with no API/MCP toggle available in this session | STILL owner-action (hard environmental cap) |
| AUDIT-011 (root-level stray files) | owner-action | Left untouched deliberately (deleting files the audit didn't create needs owner confirmation) | STILL owner-decision |

All Loop-2 changes verified locally: `pnpm run typecheck`, `api tsc --noEmit`, `pnpm run lint`
(0 errors), `pnpm run test` (9/9), `pnpm run build`, and `pnpm install --frozen-lockfile` all
pass. Commits: `a546650` (CI/tests/migrations/docs), `8515f0e` (ESLint).
