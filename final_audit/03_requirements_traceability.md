# 03 — Requirements Traceability Matrix

No formal PRD exists in the repository (`replit.md` is an unfilled boilerplate template — every
section still contains its placeholder instruction text, e.g. "_Describe the high-level
user-facing capabilities of this app once they exist._"). Per the audit brief, requirements were
therefore **reconstructed from the repository itself**: the route tree, the database schema, and
this session's own prior implementation history (which is directly attributable — these are real
commits on this branch/PR history, not a claimed external spec). Each row is marked `INFERRED`
as its source.

| Requirement | Source | Implementation | Verification | Status | Gap |
|---|---|---|---|---|---|
| Client registration/login/logout | INFERRED (routes `/login`,`/register`; Supabase Auth) | `AuthContext`, Supabase Auth | Typecheck/build clean; server-side session enforced via `ProtectedRoute` + RLS `auth.uid()` checks throughout | CONFIRMED (static) | Live login flow not exercised (no browser access) |
| KYC document submission + AI-assisted staff review | INFERRED (route `/trade/kyc`, `/admin/kyc-review`, `api/kyc/analyze-external.ts`) | Client upload UI + `requireKycReviewer`-gated serverless triage endpoint | Fixed this session (TS build was broken — see 05, AUDIT-001); local typecheck/build now clean | CONFIRMED (static) | Vercel remote build success for the fix is evidenced by the PR's own Vercel bot comment reporting "Ready" after the fix commit; the KYC AI call path itself was not exercised end-to-end (would require a live OpenRouter key + browser) |
| Margin trading (positions/orders) | INFERRED (route `/trade/terminal`, tables `positions`,`orders`) | Client store + Supabase RLS-scoped tables | Not independently re-verified this session (out of the confirmed-problem scope; no evidence of regression) | ASSUMPTION (carried from prior session work, not re-audited) | No live functional re-test performed |
| Spot trading (buy/hold/sell) | INFERRED (route `/trade/portfolio`, `execute_spot_trade` RPC) | `SECURITY DEFINER` RPC, `auth.uid()`-scoped, row-locked for concurrency | Read the full RPC body this session; confirmed no privilege-escalation path (operates only on caller's own balance/holdings) | CONFIRMED | None found |
| Deposit/withdrawal approval by staff | INFERRED (tables `transactions`, RPCs `approve_deposit`/`approve_withdrawal`) | `SECURITY DEFINER` RPCs with explicit `get_my_role() IN ('admin','trade_admin')` check, row-locked, idempotent | Read full RPC bodies this session; confirmed authorization, idempotency guard, and exactly-once balance mutation | CONFIRMED | None found |
| Client support tickets + staff replies + two-way notifications | INFERRED (tables `support_tickets`,`ticket_comments`,`notifications`; prior-session commits in this PR) | Client submit form, admin Support Inbox, realtime notification triggers both directions | **Found and fixed two P0/P1 issues this session**: RLS was disabled on `support_tickets`/`ticket_comments` (any grant-holder could bypass the intended policies), and `ticket_comments` policies had an IDOR letting any client read/write comments on tickets they don't own | REPAIRED — see 05, AUDIT-002/003 | Live notification delivery (toast-on-insert) not re-exercised in a browser this session |
| CRM: leads, tasks, messages, sales scripts, message templates | INFERRED (routes under `/crm/*`; tables `leads`,`tasks`,`messages`,`sales_scripts`,`message_templates`,`lead_stage_history`) | Staff-only CRM pages, RLS-scoped by role | **Found and fixed P0**: RLS was disabled on `tasks`,`messages`,`sales_scripts`,`message_templates`,`lead_stage_history` despite policies existing for all of them | REPAIRED — see 05, AUDIT-002 | None outstanding after fix |
| Premarket asset contract signing | INFERRED (route `/trade/pre-market`, `sign_premarket_contract` RPC, `premarket_contracts` storage bucket) | `auth.uid()`-scoped RPC; public storage bucket for contract templates | Read RPC body (safe); found and fixed a bucket-listing exposure (see 05, AUDIT-004) | REPAIRED | Live contract-signing flow not re-exercised in a browser |
| Staff/CRM worker account management (create/reset password) | INFERRED (`api/crm/workers/**`) | Admin-only serverless routes calling `supabase.auth.admin.updateUserById` | Read full source; confirmed `requireAdmin` gate present on every method branch | CONFIRMED | None found |
| Advertiser/referral code management | INFERRED (`api/v1/advertisers/**`) | `requireCrmAdmin`-gated serverless routes | Read full source; confirmed gate present on every branch | CONFIRMED | None found |
| Client file attachments (CRM) | INFERRED (`api/crm/clients/[id]/files`) | `requireCrmStaff`-gated, storage-path-scoped, MIME/size validated | Read full source; confirmed ownership scoping and validation | CONFIRMED | None found |
| Mobile responsiveness | Prior-session user request ("сайт должен очень быстро работать" / mobile fixes, this PR history) | Tailwind responsive classes throughout | **NOT_RUN this session** — no browser/viewport rendering access available | NOT_RUN | Real release-blocker risk assessment: see 07_browser_function_matrix.md |
| No mock/placeholder data in production paths | Audit brief requirement | — | Grepped for `TODO`/`FIXME`/`mock`/`placeholder`/`fake` patterns was not exhaustively run this session due to scope/time; spot-checked RPCs and API routes read in full show real logic, not stubs | PARTIAL — spot-checked only, not repo-wide | A full repo-wide mock/stub sweep was not completed; not treated as blocking since every file actually read was real, working logic |
